/**
 * Contract addresses for decx.press
 * These addresses are public information and safe to commit to version control
 */
module.exports = {
    // Mainnet deployments
    mainnet: {
        // No mainnet deployment yet
    },

    // Testnet deployments
    sepolia: {
        DecxDAG: "0x9F2Dd71C9d80d92f3896de1DA92EF3a2d5A6e16B"
    },

    // Local development (these will be different for each developer)
    localhost: {
        // These are generated dynamically when running a local node
        // Do not commit actual localhost addresses
    }
};
